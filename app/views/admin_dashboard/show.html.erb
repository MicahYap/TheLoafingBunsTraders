    <!-- Pending Users Section -->
    <div class= 'flex justify-around'>
      <section class="mb-8">
      <h2 class="text-2xl font-semibold text-gray-800 mb-4">Pending Traders</h2>
      <% if @pending_traders.any? %>
        <%= render 'pending_traders', traders: @pending_traders %>
      <% else %>
        <p class="text-gray-600">No pending traders at the moment.</p>
      <% end %>
      </section>
    </div>